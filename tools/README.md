# tools/

Auxiliary scripts. Maintenance utilities, one-off analyses, and
environment helpers that are not part of the pipeline proper.

**Status.** Empty.

Code that produces corpus records belongs in `generators/`. Code that
measures a trained model belongs in `evals/`. Library code that more than
one of those imports belongs in `src/epagoge/`.
