"""Epagoge.

A graded synthetic corpus and curriculum for training a
falsification-oriented foundation model.

This package holds the shared library code that the pipeline stages under
``generators/`` and the suites under ``evals/`` import. It is deliberately
empty at project creation. Nothing has been specified in enough detail to
implement, and placing speculative structure here would assert decisions
that have not been made.
"""

__version__ = "0.0.0"
